const seededMatchdayStories=[{type:'Matchday',published_at:'2026-08-24T15:45:00+01:00',title:'Fulham v Chelsea: West London derby under the lights',summary:'Fulham host Chelsea at Craven Cottage tonight at 8pm as the Premier League opening round concludes.',body:'Fulham and Chelsea meet at Craven Cottage tonight in a West London derby to close out the opening round of Premier League fixtures.\n\nKick-off is at 8pm, with the game shown live on Sky Sports in the UK.\n\nBoth clubs begin the new league campaign with new managers in the dugout, adding another layer to an already fierce local rivalry. Chelsea make the short trip across West London looking to start strongly, while Fulham will be hoping home advantage can give them an early-season lift.\n\nThe derby has produced plenty of tight contests in recent meetings, so this one has all the ingredients for a lively Monday-night finish to the weekend.\n\nFootball Talk question: who are you backing tonight — Fulham, Chelsea or the draw?'}];
(function addSeededMatchdayStories(){let tries=0;const timer=setInterval(()=>{tries++;try{if(typeof loadedPosts==='undefined'||typeof renderFeed==='undefined')return;const published=loadedPosts.filter(p=>['matchday','full time'].includes(String(p.type||'').toLowerCase()));seededMatchdayStories.forEach(s=>{if(!loadedPosts.some(p=>p.title===s.title))loadedPosts.push(s)});const combined=[...published,...seededMatchdayStories].filter((p,i,a)=>a.findIndex(x=>x.title===p.title)===i).sort((a,b)=>new Date(b.published_at)-new Date(a.published_at)).slice(0,6);renderFeed('matchday-posts',combined,'No matchday stories published yet.');clearInterval(timer)}catch(e){}if(tries>=12)clearInterval(timer)},500)})();

(function setupLiveNow(){
  const latest=document.getElementById('latest');
  if(!latest||document.getElementById('ft-live-now'))return;
  const LIVE=new Set(['1H','2H','ET','BT','P','LIVE','HT','INT']);
  const FINISHED=new Set(['FT','AET','PEN']);
  const approvedNames=new Map([[39,/^premier league$/i],[40,/^(efl )?championship$/i],[41,/^(efl )?league one$/i],[42,/^(efl )?league two$/i],[45,/^fa cup$/i],[48,/^(carabao cup|efl cup)$/i],[2,/^(uefa )?champions league$/i],[3,/^(uefa )?europa league$/i],[848,/^(uefa )?conference league$/i],[61,/^ligue 1$/i],[71,/^serie a$/i],[87,/^laliga$/i],[78,/^bundesliga$/i],[55,/^liga portugal$/i],[135,/^serie a$/i]]);
  let loading=false;
  const styles=document.createElement('style');styles.textContent=`#ft-live-now{max-width:1180px;margin:18px auto 8px;padding:0 20px}.ft-ln-shell{background:#0b0b0e;color:#fff;border-radius:18px;overflow:hidden;border-top:6px solid #f7c600;box-shadow:0 14px 34px rgba(0,0,0,.18)}.ft-ln-head{display:flex;justify-content:space-between;align-items:center;gap:16px;padding:18px 20px;border-bottom:1px solid #29292f}.ft-ln-kicker{font-size:12px;font-weight:1000;letter-spacing:.12em;color:#f7c600}.ft-ln-head h2{margin:2px 0 4px;font-size:clamp(25px,4vw,38px)}.ft-ln-sub{margin:0;color:#b8b8c2;font-size:13px}.ft-ln-link{background:#f7c600;color:#090909!important;text-decoration:none;font-weight:1000;padding:11px 14px;border-radius:9px;white-space:nowrap}.ft-ln-list{padding:14px 16px 16px}.ft-ln-league{margin-bottom:16px}.ft-ln-league-title{color:#f7c600;font-size:12px;font-weight:1000;letter-spacing:.06em;margin:0 0 7px}.ft-ln-game{display:grid;grid-template-columns:minmax(0,1fr) 92px minmax(0,1fr);align-items:center;gap:9px;background:#fff;color:#111;padding:11px 12px;margin-bottom:8px;border-radius:10px}.ft-ln-team{font-weight:900}.ft-ln-home{text-align:right}.ft-ln-centre{text-align:center}.ft-ln-score{display:block;font-family:'Archivo Black',sans-serif;background:#111;color:#fff;padding:7px 5px;border-radius:7px;font-size:16px}.ft-ln-status{display:block;font-size:10px;font-weight:1000;margin-top:4px;color:#777}.ft-ln-status.live{color:#c80000}.ft-ln-empty{background:#fff;color:#555;padding:18px;border-radius:10px;text-align:center}.ft-ln-footer{display:flex;justify-content:space-between;gap:12px;align-items:center;padding:0 16px 16px;color:#aaa;font-size:11px}.ft-ln-live-dot{display:inline-block;width:8px;height:8px;border-radius:50%;background:#d90000;margin-right:6px;box-shadow:0 0 0 4px rgba(217,0,0,.15)}.ft-phase-strip{display:flex;gap:7px;flex-wrap:wrap;padding:11px 16px;background:#151519;border-bottom:1px solid #29292f}.ft-phase-pill{font-size:10px;font-weight:900;letter-spacing:.04em;color:#85858f}.ft-phase-pill.active{color:#111;background:#f7c600;border-radius:999px;padding:5px 8px}@media(max-width:650px){#ft-live-now{padding:0 14px}.ft-ln-head{align-items:flex-start;flex-direction:column}.ft-ln-link{width:100%;text-align:center}.ft-ln-game{grid-template-columns:minmax(0,1fr) 68px minmax(0,1fr);padding:10px 7px}.ft-ln-team{font-size:12px}}`;document.head.appendChild(styles);
  const section=document.createElement('section');section.id='ft-live-now';section.innerHTML=`<div class="ft-ln-shell"><div class="ft-ln-head"><div><div id="ft-ln-kicker" class="ft-ln-kicker">FT LIVE</div><h2 id="ft-ln-title">MATCHDAY FEED</h2><p id="ft-ln-sub" class="ft-ln-sub">Football is happening. Talk about it. Live matchday updates and talking points at the heart of Football Talk.</p></div><a class="ft-ln-link" href="match-centre.html">OPEN MATCHDAY FEED →</a></div><div class="ft-phase-strip"><span data-phase="pre" class="ft-phase-pill">PRE-MATCH</span><span data-phase="live" class="ft-phase-pill">LIVE</span><span data-phase="reaction" class="ft-phase-pill">FULL-TIME REACTION</span><span data-phase="analysis" class="ft-phase-pill">EVENING ANALYSIS</span></div><div id="ft-ln-list" class="ft-ln-list"><div class="ft-ln-empty">Loading today’s football…</div></div><div class="ft-ln-footer"><span id="ft-ln-state">Your live route into today's football.</span><span id="ft-ln-updated"></span></div></div>`;const hero=document.querySelector('.hero');if(hero&&hero.parentNode){hero.parentNode.insertBefore(section,hero.nextSibling)}else{latest.parentNode.insertBefore(section,latest)}
  const list=document.getElementById('ft-ln-list'),title=document.getElementById('ft-ln-title'),sub=document.getElementById('ft-ln-sub'),kicker=document.getElementById('ft-ln-kicker'),state=document.getElementById('ft-ln-state'),updated=document.getElementById('ft-ln-updated');
  const londonDate=d=>new Intl.DateTimeFormat('en-CA',{timeZone:'Europe/London',year:'numeric',month:'2-digit',day:'2-digit'}).format(d);const kickOff=v=>new Intl.DateTimeFormat('en-GB',{timeZone:'Europe/London',hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date(v));const leagueWanted=l=>{const rule=approvedNames.get(Number(l.id));return Boolean(rule&&rule.test(String(l.name||'').trim()))};const statusText=f=>{const s=String(f.status||'NS').toUpperCase();if(s==='HT')return'HT';if(LIVE.has(s))return f.elapsed?`LIVE · ${f.elapsed}′`:'LIVE';if(FINISHED.has(s))return s==='FT'?'FT':s;return kickOff(f.date)};const scoreText=f=>{const s=String(f.status||'NS').toUpperCase();return(LIVE.has(s)||FINISHED.has(s))?`${f.homeGoals??0}–${f.awayGoals??0}`:kickOff(f.date)};
  const mergeLeagues=(...sets)=>{const map=new Map();sets.flat().forEach(l=>{if(!l)return;const key=`${Number(l.id)||0}:${String(l.name||'').toLowerCase()}`;if(!map.has(key))map.set(key,{...l,fixtures:[]});const out=map.get(key),seen=new Set(out.fixtures.map(f=>String(f.id||`${f.date}-${f.home}-${f.away}`)));(l.fixtures||[]).forEach(f=>{const fk=String(f.id||`${f.date}-${f.home}-${f.away}`);if(!seen.has(fk)){seen.add(fk);out.fixtures.push(f)}})});return[...map.values()]};
  function setPhase(p){document.querySelectorAll('.ft-phase-pill').forEach(x=>x.classList.toggle('active',x.dataset.phase===p));document.body.dataset.matchdayPhase=p;window.dispatchEvent(new CustomEvent('ft:matchday-phase',{detail:{phase:p}}));}
  function render(leagues){const now=Date.now(),today=londonDate(new Date());const filtered=(leagues||[]).filter(leagueWanted).map(l=>({...l,fixtures:(l.fixtures||[]).filter(f=>f.date&&londonDate(new Date(f.date))===today)})).filter(l=>l.fixtures.length);const live=filtered.map(l=>({...l,fixtures:l.fixtures.filter(f=>LIVE.has(String(f.status||'').toUpperCase()))})).filter(l=>l.fixtures.length);const upcoming=filtered.map(l=>({...l,fixtures:l.fixtures.filter(f=>!LIVE.has(String(f.status||'').toUpperCase())&&!FINISHED.has(String(f.status||'').toUpperCase())&&new Date(f.date).getTime()>=now).sort((a,b)=>new Date(a.date)-new Date(b.date)).slice(0,6)})).filter(l=>l.fixtures.length);const finished=filtered.map(l=>({...l,fixtures:l.fixtures.filter(f=>FINISHED.has(String(f.status||'').toUpperCase())).sort((a,b)=>(b.timestamp||0)-(a.timestamp||0)).slice(0,6)})).filter(l=>l.fixtures.length);let showing=[],phase='pre';
    if(live.length){showing=live;phase='live';kicker.innerHTML='<span class="ft-ln-live-dot"></span>FT LIVE';title.textContent='MATCHDAY FEED · LIVE NOW';sub.textContent='Football is happening now — live scores and matchday talking points take over Football Talk.';state.textContent='Live football is at the heart of the homepage.';}
    else if(upcoming.length){showing=upcoming;phase='pre';kicker.textContent='FT LIVE';title.textContent='MATCHDAY FEED · COMING UP';sub.textContent='Today’s next featured kick-offs, ready for live coverage and fan discussion.';state.textContent='The feed switches into live mode automatically when a featured match starts.';}
    else if(finished.length){showing=finished;const lastEnd=Math.max(...finished.flatMap(l=>l.fixtures).map(f=>(Number(f.timestamp)||new Date(f.date).getTime()/1000)*1000+2*60*60*1000));const hours=new Date(new Date().toLocaleString('en-US',{timeZone:'Europe/London'})).getHours();phase=(now-lastEnd<90*60*1000)?'reaction':'analysis';kicker.textContent='FT LIVE';title.textContent=phase==='reaction'?'MATCHDAY FEED · FULL-TIME':'MATCHDAY FEED · REVIEW';sub.textContent=phase==='reaction'?'The final whistle has gone — results and immediate reaction take priority.':'Today’s featured football is complete — catch up on results, reaction and the talking points.';state.textContent=phase==='reaction'?'Reaction mode follows the final whistle before deeper analysis takes over.':'The Matchday Feed stays your route into the day’s football.';}
    else{kicker.textContent='FT LIVE';title.textContent='MATCHDAY FEED';sub.textContent='No featured matches are listed today — use the Matchday Feed for upcoming fixtures and the next football talking points.';state.textContent='Open the feed for the next scheduled football.';}
    setPhase(phase);list.replaceChildren();if(!showing.length){const e=document.createElement('div');e.className='ft-ln-empty';e.textContent='No featured fixtures remain today. Open the Matchday Feed for the next scheduled games.';list.appendChild(e);}showing.slice(0,6).forEach(l=>{const box=document.createElement('div');box.className='ft-ln-league';const h=document.createElement('div');h.className='ft-ln-league-title';h.textContent=l.name||'Football';box.appendChild(h);l.fixtures.slice(0,6).forEach(f=>{const s=String(f.status||'NS').toUpperCase(),row=document.createElement('div');row.className='ft-ln-game';row.innerHTML=`<div class="ft-ln-team ft-ln-home"></div><div class="ft-ln-centre"><span class="ft-ln-score"></span><span class="ft-ln-status${LIVE.has(s)?' live':''}"></span></div><div class="ft-ln-team ft-ln-away"></div>`;row.children[0].textContent=f.home||'';row.querySelector('.ft-ln-score').textContent=scoreText(f);row.querySelector('.ft-ln-status').textContent=statusText(f);row.children[2].textContent=f.away||'';box.appendChild(row)});list.appendChild(box)});updated.textContent=`UPDATED ${new Intl.DateTimeFormat('en-GB',{timeZone:'Europe/London',hour:'2-digit',minute:'2-digit',second:'2-digit'}).format(new Date())}`;}
  async function getJson(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json()}async function load(){if(loading)return;loading=true;try{const today=londonDate(new Date());const results=await Promise.allSettled([getJson(`/api/fixtures?date=${today}`),getJson('/api/fixtures'),getJson('/api/fixtures?predictions=1')]);const leagues=[];results.forEach(r=>{if(r.status==='fulfilled')leagues.push(...(r.value.leagues||[]))});if(!leagues.length)throw new Error('No fixture data');render(mergeLeagues(leagues));}catch(e){if(!list.querySelector('.ft-ln-game'))list.innerHTML='<div class="ft-ln-empty">Live match data is temporarily unavailable. The full Matchday Feed remains available.</div>';}finally{loading=false}}load();setInterval(load,30000);document.addEventListener('visibilitychange',()=>{if(!document.hidden)load()});
})();
